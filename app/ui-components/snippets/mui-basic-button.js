export const muiBasicButton = `import Button from '@mui/material/Button';

function MyComponent() {
  return (
    <Button variant="contained" color="primary">
      Material UI Button
    </Button>
  );
}`;
