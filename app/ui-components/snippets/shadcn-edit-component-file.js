export const shadcnEditComponentFile = `// components/ui/button.tsx - Edit directly
const buttonVariants = cva(
  "inline-flex items-center justify-center rounded-lg", 
  {
    variants: {
      variant: {
        default: "bg-red-600 hover:bg-red-700"
      }
    }
  }
)`;
