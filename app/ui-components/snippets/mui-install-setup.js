export const muiInstallSetup = `npm install @mui/material @emotion/react

// Theme setup in layout
import { ThemeProvider, createTheme } from '@mui/material/styles'
const theme = createTheme({ /* config */ })`;
