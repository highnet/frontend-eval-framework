export const tanstackRouterFileBasedDefinition = `// routes/index.tsx - Automatic / route
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/')({
  component: () => <div>Home Page</div>`;
